# smoke_test.dmc — minimal `dmc test` fixture.

fn square(x: i64) -> i64 {
    x * x
}

fn test_scalar_math() -> bool {
    square(9) == 81
}

fn test_tensor_sum() -> bool {
    let t = [1.0, 2.0, 3.0, 4.0]
    sum(t) == 10.0
}

fn test_nil_passes() -> nil {
    nil
}
